#include <stdio.h>

int main(){
    chave chave = 50;
    int v[] = { 10, 20, 30 };
    int n = 3;
    int;

    for (i = 0; i < n; i++) { //N + 1
        if (v[i] == chave) { //N
            printf("Encontro em %d\n", i);
            retornar 0;
        }
    }

    printf("Não encontrei\n");
    retornar 0;
}
